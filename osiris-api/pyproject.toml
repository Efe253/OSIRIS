[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "osiris-api"
version = "0.1.0"
description = "OSIRIS REST API sunucusu"
requires-python = ">=3.11"
dependencies = [
    "osiris-sdk>=0.1.0",
    "osiris-collector>=0.1.0",
    "osiris-graph>=0.1.0",
    "osiris-query>=0.1.0",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.0",
    "psycopg[binary]>=3.1",
]

[project.scripts]
osiris-api = "osiris_api.main:run"

[tool.setuptools.packages.find]
include = ["osiris_api*"]
